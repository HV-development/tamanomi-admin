# ==============================================
# API設定
# ==============================================

# フロントエンドからAPIへの接続URL（ブラウザで公開される）1
NEXT_PUBLIC_API_BASE_URL=http://localhost:3002/api/v1

# サーバーサイドAPI設定（APIルート用）
# Next.jsのAPIルートからバックエンドAPIへの接続URL
# NEXT_PUBLIC_API_BASE_URLと同じ値を設定
API_BASE_URL=http://localhost:3002/api/v1

# ==============================================
# 認証設定
# ==============================================

# JWT署名用の秘密鍵（tamanomi-apiと同じ値を使用する必要があります）
# 本番環境では強力なランダムな文字列を使用してください
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here

# ==============================================
# Next.js開発環境設定
# ==============================================

# Next.jsのテレメトリーデータ収集を無効化
# 開発・本番環境ともに1を設定（セキュリティ・プライバシーのため）
NEXT_TELEMETRY_DISABLED=1

# ファイル監視用のポーリング機能を有効化（開発環境専用）
# DockerやWSL環境でファイル変更が検知されない場合にtrueに設定
# 本番環境では設定不要（デフォルトで無効）
WATCHPACK_POLLING=true

# Next.jsのキャッシュ機能を無効化（開発環境専用）
# 開発時にキャッシュによる問題を回避するため
# 本番環境では設定しない（パフォーマンス向上のためキャッシュを有効化）
NEXT_CACHE_DISABLED=1

# ==============================================
# 本番環境での推奨設定
# ==============================================
# 
# 本番環境（.env.production）では以下のように設定してください：
# 
# NEXT_TELEMETRY_DISABLED=1
# # WATCHPACK_POLLING は設定しない（不要）
# # NEXT_CACHE_DISABLED は設定しない（パフォーマンス向上のため）
# NEXT_PUBLIC_API_BASE_URL=https://your-api-domain.com/api/v1
# API_BASE_URL=https://your-api-domain.com/api/v1
# JWT_SECRET=your-production-jwt-secret
# NODE_ENV=production
